// @testing-library/react-native v14 automatically extends expect with custom
// matchers (toBeOnTheScreen, etc.) when the package is imported.
// There is no separate /extend-expect subpath in v14 — the main entry handles it.
import '@testing-library/react-native';
