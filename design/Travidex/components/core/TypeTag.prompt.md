Blue classification chip for a sight's type/category tags.

```jsx
<TypeTag label="Historic" icon={<Landmark size={13} />} />
<TypeTag label="Scenic" />
```

Always blue (the `info` semantic). Keep labels one word where possible; render a row of them under a sight title.
