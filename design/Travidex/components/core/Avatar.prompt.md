Circular avatar — user photo, or initials fallback on a neutral surface.

```jsx
<Avatar src={user.avatar} name="Mira Vale" size={40} />
<Avatar name="Mira Vale" size={56} ring />
```

Props: `src`, `name` (initials + alt), `size` (px), `ring` + `ringColor` (accent ring for own profile / active).
