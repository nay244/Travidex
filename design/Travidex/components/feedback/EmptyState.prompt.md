Centered empty/zero state.

```jsx
<EmptyState
  icon={<MapPin size={22}/>}
  title="No finds yet"
  message="Tap a pin to read its hint, then log your first find."
  action={<Button variant="primary" size="sm">Explore the map</Button>}
/>
```

Use for empty feeds, no-finds, unsupported cities, search-with-no-results. Keep copy a forward nudge.
